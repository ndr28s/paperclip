auto-install-peers=true
# Hoist all deps to top-level node_modules. Required on Windows for React Native
# Android NDK builds because pnpm's default .pnpm/<long-hash>/ store paths exceed
# Ninja's 260-char limit. Trade-off: loses pnpm's strict dependency isolation.
node-linker=hoisted
